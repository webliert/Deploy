# Zero Policy Configuration
# 注意：机器人特定参数（kp, kd, zero_pos等）从robot_profile获取

# 策略控制参数
interp_step: 0.001 # 插值速度
interp_max: 0.9 # 插值最大值

# 策略使用的关节组（引用robot_profile中的定义）
joint_groups:
  - "legs.left"
  - "legs.right"
  - "arms.left"
  - "arms.right"
